# PLAN_dependency-version-bump-2026-09-02 — nextjs-shared

## Title
Dependency version bump

## Plan
- [x] Bump dependency versions in package.json (done by the user before this run)
- [x] Run #reinstall — npm install --legacy-peer-deps, npx tsc --noEmit, npm run build all passed (Next 16.2.9, unchanged)
- [x] Run #commit — combined with PLAN_versions-per-row-sync in one commit per user instruction ("commit both plans and all changes")

## Changes
- Dependency versions bumped in package.json; package-lock.json regenerated by #reinstall.
